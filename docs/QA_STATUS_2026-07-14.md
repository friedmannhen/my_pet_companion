# QA status & readiness analysis — 2026-07-14 (overnight pass)

> **Addendum 2 (overnight, after first play-test round) — 8 fixes shipped.**
> User play-tested the online-play UX pass and reported 8 issues, all fixed
> while they slept:
> 1. **Room-invite spam guard + mutual cancel**: the 🌐 Invite button now
>    goes to a disabled "⏳ Invited…" state per friend and can't be spammed;
>    clears when they join, when they explicitly decline (a new
>    `room_invite_declined` control-signal notification round-trips back to
>    the inviter), or after a 60s timeout on either side.
> 2. Friend rows now show **"✅ In your room"** instead of an invite button
>    once that friend is actually present (`room.members`).
> 3. **Removed the exact-landing preview** in Target Toss (was "kinda
>    cheating" — showed exactly where the puck would stop before throwing).
>    Replaced with a strength-only **power bar** (turns red past 85% pull).
> 4. **The puck is now each player's own pet sprite** (~45px,
>    `PUCK_SIZE`), not a generic emoji — reduced spin so it stays
>    recognizable mid-slide instead of tumbling like a true physics puck.
> 5. **Distance/result is hidden until the puck actually stops** — was
>    previously revealed the instant the throw resolved, before the slide
>    animation even played.
> 6. **Landed pets stay on the ice** (persistent sprite at the landing
>    spot) until the round changes, instead of a small dot marker that
>    vanished. Also hardened pet-dragging/menu lockout during any minigame
>    (`inMinigame` flag in GameView) as belt-and-suspenders on top of the
>    full-screen overlay that already structurally blocks those clicks.
> 7. **Scoring changed from "most rounds won" to golf-style "lowest total
>    distance across all rounds wins"** — a real bug in the old system
>    (someone could win 2 close rounds and lose to a landslide loss in the
>    third). New `computeTotalDistances`/`totalDistanceLeaders` in
>    pet-core, with a `MISS_PENALTY_DISTANCE` (200) charged for any miss so
>    skipping is never good strategy. Added a `(?)` how-to-play tooltip to
>    the HUD explaining the new rules. 8 new pet-core tests, including one
>    that specifically constructs a case where the old and new scoring
>    systems would disagree (to prove the rewrite actually changed
>    behavior, not just pass by coincidence).
> 8. **Target/launcher position is now randomized vertically each round**
>    (seeded — `arenaLayoutForTurn` in pet-core, mulberry32-based), same
>    position for every player within a round for fairness, new position
>    next round. The seed is generated once by the host and broadcast in
>    `mg-start`; no per-round network traffic needed. Handled a subtle
>    timing bug here: the reducer's `currentTossTurn` advances to the NEXT
>    round the instant a throw event applies, but that throw's puck is
>    still mid-flight using the OLD round's coordinates — so the arena's
>    displayed layout deliberately lags one throw behind (via a
>    `layoutRoundKey` state only advanced inside the slide-completion
>    callback), or the target/launcher would visibly teleport out from
>    under a still-sliding puck.
>
> Diagnostics after this round: typecheck clean, **91/91 tests** (8 new),
> production build OK, renderer boots error-free.
>
> **Needs the user:** another play-test pass, specifically exercising items
> 1/2 (invite spam + already-in-room state) and 7/8 (new scoring + random
> positions) since those are the most behaviorally different from what was
> just tested. No new Supabase migrations were needed for this round — the
> `npx supabase db push` from earlier is still the only outstanding
> infra step.

> **Addendum (later on 2026-07-14) — online-play UX pass shipped.** All 10+2
> review items implemented: realtime-only notification system
> (`useNotifications.ts`: pet-bubble toasts + room-invite Join/Dismiss
> banner), friend→room invites, instant 6-char code join that auto-enters
> the room, always-visible group cards with inline 🟢 presence badges
> (`GroupRow`), full-screen RPS modal (5s pick countdown → cancel naming
> the no-picker → 3s reveal drumroll → winner-pet wiggle), Target Toss
> physics rewritten to straight-line curling slides (`curlPhysics.ts`,
> aim preview = exact stop point; overshooting the screen = a miss,
> `distance: null` — also fixed a `?? 0` coercion that would have scored a
> miss as a bullseye), mid-game cancel when players dwindle to ≤1, 3s
> per-turn get-ready banners + bigger HUD, markers deferred until the puck
> stops, Follow-Me now closes the radial menu, SideDock two-row header
> (removed the overflowing ✕), and the installer icon
> (`build/icon.png` from house.png, 5× nearest-neighbor upscale — source is
> only 93×92, so a hi-res original would sharpen large shortcuts later).
> Diagnostics after the pass: typecheck clean, 83/83 tests, production
> build OK, renderer boots error-free. New known-issue: notification
> `sendTo` may drop a message if two sends to the same target race within
> ~1.5s (transient-channel reuse before SUBSCRIBED) — dev-scale acceptable.

Scope: full review of the uncommitted feature set (groups management, history
log, egg picker, warm mode, egg dormancy, display scaling, friends, RPS,
Target Toss, leaderboard filters) ahead of multi-user testing.

## Diagnostics (all passing)

| check | result |
|---|---|
| `pnpm -r typecheck` | clean (pet-core + desktop) |
| `pnpm -r test` | 83/83 (16 cover Target Toss turn/reducer logic, 3 cover egg dormancy) |
| `pnpm build:desktop` (production) | builds in ~3s; renderer bundle 1.92 MB (fine for Electron, no code-splitting needed) |
| Browser-mock boot | zero console errors at sign-in card |
| `supabase migration list --linked` | in sync through `20260712010000`; **`20260713000000_harden_functions` + `20260713010000_minigame_scores` pending `db push`** |
| `supabase db advisors` | remaining WARNs are perf-at-scale lint (`auth_rls_initplan`, `multiple_permissive_policies`) — deliberately deferred; the actionable security items are fixed in the pending harden migration |

## Bugs found & fixed in this pass

1. **Ghost room presence (race)** — leaving the room while `join()` was
   still waiting to free a stale observer channel let the deferred
   subscribe run anyway, tracking presence in a room the UI had left.
   Fixed with a join-generation guard (`useRoom.ts`).
2. **Friends: declined request permanently blocked re-requesting** — the
   declined row shares the PK with any new insert, and RLS forbids the
   requester updating it. `request()` now deletes its own declined row
   first (`useFriends.ts`).
3. **History logged failures as successes** — "Created/Deleted/Left group"
   entries were written even when the RPC failed (offline, RLS).
   `leave`/`deleteGroup` now return success booleans and SideDock gates all
   group history logging on the actual result.
4. **RPS invite allowed during a Target Toss lobby/game** — now mutually
   exclusive in both directions.

## Known issues (accepted for now, ranked)

1. **Realtime channel auth unhardened** *(pre-existing, highest-value fix
   before going beyond friends-scale)* — channel topics embed raw group
   UUIDs; anyone told an id could subscribe and spoof presence/broadcasts.
   Also means minigame/battle events are trust-based between clients.
2. **Turn-skip race (low probability)** — a Target Toss throw sent right at
   the 15s AFK boundary can cross with the authority's skip; both carry the
   same `seq`, so clients keep whichever arrived first → possible desync.
   Needs presence flap + exact timing. Future fix: totally ordered event
   chain (hash/ack) or a lightweight resync broadcast.
3. **Deleting a group doesn't kick live members** — their channel survives;
   they idle in a ghost room until they leave. Needs a room-closed
   broadcast or #1.
4. **Ties in `minigame_scores` are indistinguishable from losses**
   (games_played+1, games_won+0; no ties column). By-schema; fine until a
   win-rate UI exists.
5. **Egg cosmetics are placeholders** — the picker's 3 eggs are tints; the
   chosen tint isn't persisted and gameplay is always `cat`. Blocked on art
   for more `PetType`s.
6. **Marker-before-landing** — a Target Toss landing marker appears the
   instant the event applies, ~1s before the replayed ball visually lands.
7. **localStorage save write is undebounced** — every action serializes the
   full save (incl. ≤200 history entries). No observed jank; if it appears,
   debounce the write like the cloud push (2s).
8. **Quest sync is partial cross-device** (`rewardHistory`, `cap`,
   `lastQuestMenuOpenedAt` reset on device switch) and the
   `quest_reward_history` DB table has no writers — decide to wire or drop.
9. **Duplicate-direction friends rows possible** (A→B declined + B→A
   pending shows twice) — cosmetic at dev scale.

## Multi-user test checklist (what to actually exercise)

Setup: 2–3 test accounts, two browser-mock tabs (pet-game-quests-testing
skill technique) or two machines. **Run `npx supabase db push` first.**

- [ ] Fresh signup → egg picker appears once → warm mode (lamp → glow
      cursor → hold on egg) → hatch → scale springs to 0.7 after jump-out.
- [ ] Room: presence count in groups menu (expanded row) matches reality
      while a second account joins/leaves; positions/chat/emotes flow.
- [ ] Battle: challenge → accept → identical result both sides; history
      entries show clamp-aware deltas.
- [ ] RPS: 🎮 invite → both pick → identical outcome; tie path; abandon ✕;
      `minigame_scores` rows update (needs db push).
- [ ] Target Toss: lobby cap turn-away (3rd account, cap 2) → ready → start
      → 3 rounds round-robin → one deliberate 15s AFK skip → one client
      closes mid-game (auto-skips continue) → forced tie → sudden death →
      winner banner → scores recorded once per participant.
- [ ] Host leaves lobby pre-start → lobby auto-closes on other clients.
- [ ] Groups: owner delete (confirm inline) → disappears everywhere after
      refresh; non-owner cannot see delete; Global undeletable.
- [ ] Friends: search → request → accept on 2nd account (badge shows) →
      remove → re-request after a decline (regression for fix #2).
- [ ] Same machine, account switch: no save/history bleed (SAVE_OWNER_KEY),
      no 460-carePoints-style seeding.
- [ ] Windows taskbar icon stays hidden when settings/groups/friends focus
      a text input (real Electron run, not browser mock).

## Suggested next priorities

1. `npx supabase db push` (user) — unblocks score persistence + hardening.
2. Realtime authorization (private channels) — the one structural gap
   before testing with strangers rather than friends.
3. Decide `quest_reward_history` (wire it or drop it).
4. Art pipeline for a second real `PetType` — unlocks the egg picker being
   a real choice + `allPetsHatched` achievement meaning something.
5. Auto-updater / release flow re-test after these features settle.
